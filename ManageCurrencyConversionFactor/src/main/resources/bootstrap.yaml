# Properties active for ALL profiles:
---
spring:
  cloud:
    config:
      uri:   # Expect the Spring Cloud Config server to be running on localhost, port 8001.  
server:
  port: 9090   # Select any available port if neither port nor server port are specified.


# Depending on which profile is active, the application gives 
# itself a different name when registering with Eureka
---
spring:
  application:
    name: conversionFactor
  
         
    